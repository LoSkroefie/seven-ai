# Legacy Seven AI v3.2.x (archived)

This directory is the **pre–Seven Real** codebase: large “sentience architecture,” multiple entrypoints, and marketing that often outran the implementation.

**Do not use this as the product.**

Supported agent:

```bat
cd ../..
python -m seven
```

See root `README.md` and package `seven/`.
