# Seven Real v4 — lean, installable deps
# Core (required for local agent)
requests>=2.31.0
psutil>=5.9.0

# Screen / desktop control (L4)
pyautogui>=0.9.54
Pillow>=10.0.0
pyperclip>=1.8.0

# Desktop tray (optional but recommended for --gui)
pystray>=0.19.0

# Vision (webcam)
opencv-python>=4.8.0
numpy>=1.24.0

# Voice (optional — enable with: python -m seven --voice)
edge-tts>=6.1.0
pygame>=2.5.0
pyttsx3>=2.90
SpeechRecognition>=3.10.0
# openai-whisper>=20231117   # large; install when using local STT
# pyaudio>=0.2.13            # Windows: pipwin install pyaudio if needed

# Robotics (optional)
# pyserial>=3.5

# API server (optional)
# fastapi>=0.110.0
# uvicorn>=0.27.0

# Dev
pytest>=8.0.0
